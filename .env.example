# Local run against your own Postgres. Leave RECON_PASSWORD empty to run without a login on http://127.0.0.1:8790
RECON_DSN=host=localhost dbname=kredo_recon user=ashwin
RECON_PASSWORD=
# Cloud (Render sets these for you via render.yaml):
# DATABASE_URL=postgresql://...        RECON_PASSWORD=<10+ chars>        RECON_SECRET=<24+ random chars>
